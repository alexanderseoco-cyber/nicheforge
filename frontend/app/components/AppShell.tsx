"use client";
import Link from "next/link";
import {useState} from "react";
const groups=[['RESEARCH',[['Search Volume','/research/search-volume'],['City Research','/research/cities'],['Keyword Library','/research/keywords']]],['RANK & RENT',[['Niche Validator','/rank-rent/validator'],['Validation Runs','/rank-rent/runs'],['Candidates','/rank-rent/candidates'],['Results','/rank-rent/results']]],['DATA',[['Evidence','/data/evidence'],['Geo Targets','/data/geo-targets']]],['SYSTEM',[['Providers','/system/providers'],['Settings','/system/settings']]]];
export function AppShell({children,active}:{children:React.ReactNode;active?:string}){const [open,setOpen]=useState(false);return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><span className="brand-mark">N</span><div><strong>NicheForge</strong><small>SEO Intelligence</small></div></div><nav aria-label="Primary navigation"><Link className={active==='Overview'?'nav-item active':'nav-item'} href="/">◈ <span>Overview</span></Link>{groups.map(([label,items])=><div className="nav-group" key={label as string}><div className="nav-label">{label}</div>{(items as string[][]).map(([name,href])=><Link key={href} className={active===name?'nav-item active':'nav-item'} href={href}>{icon(name)}<span>{name}</span></Link>)}</div>)}</nav></aside><main className="main"><button className="mobile-menu" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">☰</button>{children}<footer>NicheForge · Evidence-led SEO intelligence</footer></main></div>}
function icon(name:string){return <span className="nav-icon" aria-hidden="true">{name==='Search Volume'?'⌕':name==='Niche Validator'?'✓':name==='Evidence'?'◫':'○'}</span>}
