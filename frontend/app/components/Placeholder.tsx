import Link from "next/link"; import {AppShell} from "./AppShell";
export function Placeholder({active,title,description}:{active:string;title:string;description:string}){return <AppShell active={active}><div className="page-head"><div><p className="eyebrow">NicheForge</p><h1>{title}</h1><p className="muted">{description}</p></div></div><section className="card empty large"><div className="empty-mark">+</div><h2>Workspace coming next</h2><p className="muted">This area is prepared for the next UI phase.</p><Link className="button secondary" href="/">Back to Overview</Link></section></AppShell>}
